# Cuckoo.ai - Development / test dependencies
# Install with:  pip install -r requirements-dev.txt
# (Pulls in the runtime deps too; async agents need pytest-asyncio.)

-r requirements.txt

pytest>=7.0.0,<9.0.0
pytest-asyncio>=0.21.0
