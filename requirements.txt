# Cuckoo.ai - Runtime dependencies
# EY Techathon 6.0 - Round 2
#
# Only the packages the app actually imports today. Aspirational libraries for
# the planned production build (LLM / data / DB / vector store) are listed but
# commented out so installs stay lean and deploys stay fast. Uncomment a line
# when the corresponding code is actually wired in.
# Dev/test tools live in requirements-dev.txt.

# --- Runtime (required) ---
streamlit>=1.28.0,<2.0.0    # Web UI (app.py)
reportlab>=4.0.0,<5.0.0     # PDF report generation (src/reports/pdf_generator.py)

# --- Planned for production (not imported yet) ---
# openai>=1.0.0             # real LLM calls (currently mocked)
# langchain>=0.1.0
# langchain-openai>=0.0.2
# pandas>=2.0.0             # data processing
# numpy>=1.24.0
# pydantic>=2.0.0           # schema validation
# python-dotenv>=1.0.0      # load API keys from .env
# asyncio-throttle>=1.0.0   # rate-limit real API calls
# psycopg2-binary>=2.9.0    # Postgres (institutional memory store)
# sqlalchemy>=2.0.0
# faiss-cpu>=1.7.0          # vector store for semantic archive search
# pgvector>=0.2.0
